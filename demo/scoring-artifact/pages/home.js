import { Scent } from '../lib.js';
const { defineComponent } = Scent;

/*
 * 进入首页
 * 判断有无当前未结束的对局记录
 * 若有，则跳转到对局页面
 * 若无，则显示开启对局组件
 */
export const home = defineComponent({
    template: `
    
    `
})